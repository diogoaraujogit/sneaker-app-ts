import React from 'react';
import Layout from '../../components/Layout';

import { Container } from './styles';

const Receipt: React.FC = () => {
  return (
    <Layout title="Review and Confirmation">
      <Container>Receipt</Container>
    </Layout>
  );
};

export default Receipt;
